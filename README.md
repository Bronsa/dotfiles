insert password in `.authinfo`, `.mpdscribble/mpdscribble.conf`, `.offlineimaprc`, `.emacs.d/config/erc-conf.el`, `.lein/credentials.clj`

add `stumpish` in `bin`

run `make` in `.emacs.d/lib/auto-complete-clang-async`

install `mu` system-wise
